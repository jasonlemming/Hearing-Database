# Vercel deployment requirements (web UI only - no ingestion dependencies)
requests>=2.31.0
aiohttp>=3.9.0
pydantic>=2.0.0
python-dotenv>=1.0.0
click>=8.1.0
pyyaml>=6.0
flask>=3.0.0
pydantic-settings>=2.0.0
sqlalchemy>=2.0.0

# PostgreSQL database support
psycopg2-binary>=2.9.9

# Lightweight HTML parsing for display only (no Playwright/browser automation)
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Note: Playwright, PyPDF2, and tqdm are NOT included - only needed for local ingestion
